node_modules
dist
.env
.DS_Store
coverage
.vercel
.env*.local
venv
.agent
.agent/workflows

# Ignore all large CSVs to prevent GitHub rejection (using Releases instead)
*.csv
public/datasets/*.csv
public/datasets/split_data/*.csv
split_data/
public/datasets/master_parts/*.csv

# Allow keeping the empty structure or small configs if needed, but blocking data
__pycache__/
